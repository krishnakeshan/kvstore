package command.response.kv;

import command.response.CommandResponse;

public class DeleteKeyCommandResponse extends CommandResponse {
    public DeleteKeyCommandResponse(boolean successful) {
        super(successful);
    }
}
