package command.response.kv;

import command.response.CommandResponse;

public class PutKeyCommandResponse extends CommandResponse {
    public PutKeyCommandResponse(boolean successful) {
        super(successful);
    }
}
